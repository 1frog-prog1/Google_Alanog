package com.example.domain.usecase.task_usecase

import com.example.domain.repository.TaskRepository

class addTaskDate(private val taskRepository : TaskRepository) {
    // todo: add task date usecase
}